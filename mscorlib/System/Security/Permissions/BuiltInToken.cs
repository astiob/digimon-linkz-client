using System;

namespace System.Security.Permissions
{
	internal enum BuiltInToken
	{
		Environment,
		FileDialog,
		FileIO,
		IsolatedStorageFile,
		Reflection,
		Registry,
		Security,
		UI,
		Principal,
		HostProtection,
		PublisherIdentity,
		SiteIdentity,
		StrongNameIdentity,
		UrlIdentity,
		ZoneIdentity,
		GacIdentity,
		KeyContainer
	}
}
